# Docker Compose Templates

Ready-to-use docker-compose files for common development stacks.

## Templates
- `postgres/` - PostgreSQL + pgAdmin
- `mongo/` - MongoDB + Mongo Express
- `redis/` - Redis + Redis Commander

## Usage
```bash
cd postgres/
docker-compose up -d
```
